# Specification:
# dict = {"sport name":{"Country1":32, "Country2":78}]} OR
# dict = {"sport name": [[Country1,Country2],[32,78]]}

# 1. A function that receives a sport name and returns the top 3 countries (gold, silver, bronze medal receivers)
# 2. A function that calculates the total points (sum) each country obtained -> tuple(["Country1","Country2"][66,23])
# 3. A function that receives a tuple from function 2 and another tuple (last years results) and computes the percentage progress of each country.eval

# Input comes from a csv file
# Maybe try having a country as a class?
